import { Alert } from "react-bootstrap";

const Footer = () => {
    return (
        <Alert variant="success" style={{ textAlign: 'center' }}>
            Job Website
        </Alert>
    );
}

export default Footer;
